{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "description": [
    "minimumReleaseAge mirrors pnpm's own policy. pnpm 11 refuses to install a package published within the last 24 hours, and `--frozen-lockfile` turns that into ERR_PNPM_MINIMUM_RELEASE_AGE_VIOLATION, so an update proposed sooner than that cannot pass CI no matter what it contains. Three days rather than one because the entries that trip the check are usually the update's siblings, published as a set alongside it, and Renovate ages only the package it names."
  ],
  "minimumReleaseAge": "3 days",
  "rangeStrategy": "pin",
  "packageRules": [
    {
      "description": "Never touch `engines.node`. It declares the range of runtimes a package SUPPORTS, not a dependency to pin: the global `rangeStrategy: pin` rewrites it to the exact version Renovate happened to run on, which warns on install for every consumer on another Node and hard-fails under `engine-strict`.",
      "matchDepTypes": ["engines"],
      "enabled": false
    },
    {
      "description": "Patch, minor and pin bumps merge on their own once CI is green. These are the ones `release.yml` turns into an automatic patch release. `pin` is its own update type in Renovate rather than a flavour of patch, so it has to be named here or the initial pinning PR sits open forever.",
      "matchDepTypes": ["dependencies", "devDependencies"],
      "matchUpdateTypes": ["patch", "minor", "pin"],
      "automerge": true,
      "automergeStrategy": "squash"
    },
    {
      "description": "Majors are reviewed by hand, and merging one by hand means no automatic release either.",
      "matchDepTypes": ["dependencies", "devDependencies"],
      "matchUpdateTypes": ["major"],
      "automerge": false
    },
    {
      "description": "A GitHub Actions bump changes nothing a consumer installs, so it merges on its own and never cuts a release. Its updates arrive under the `github-actions` manager rather than as a dependency depType, so the rules above do not reach them. Majors still wait for a person.",
      "matchManagers": ["github-actions"],
      "matchUpdateTypes": ["patch", "minor", "pin", "pinDigest", "digest"],
      "automerge": true,
      "automergeStrategy": "squash"
    },
    {
      "description": "changesets/action v2 refuses to run against Changesets CLI v2, which is what this repo has: it exits with `This version of the Changesets action is designed to work with Changesets CLI v3`. The action major is blocked until `@changesets/cli` is on v3, and a PR for it would look safe because no CI job exercises the release path.",
      "matchPackageNames": ["changesets/action"],
      "matchUpdateTypes": ["major"],
      "enabled": false
    },
    {
      "description": "A peer range is the compatibility window this package advertises. Widen it rather than pinning it, and never without a human deciding the window is really that wide.",
      "matchDepTypes": ["peerDependencies"],
      "rangeStrategy": "widen",
      "automerge": false
    }
  ],
  "lockFileMaintenance": {
    "enabled": true,
    "automerge": true
  },
  "ignorePaths": ["**/dist/**", "**/node_modules/**", "**/.next/**", "**/build/**"]
}
