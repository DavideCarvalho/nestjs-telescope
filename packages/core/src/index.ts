/**
 * @dudousxd/nestjs-telescope — core public API.
 *
 * The architectural contract (Entry, Watcher, StorageProvider, Recorder,
 * TelescopeModule options, correlation model) is specified in ../../DESIGN.md.
 * Implementation lands here, package by package, per the implementation plan.
 */

export const TELESCOPE_VERSION = '0.0.0';
