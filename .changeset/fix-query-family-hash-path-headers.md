---
"@dudousxd/nestjs-telescope": patch
---

Correct the file-path header comments in `query-family-hash.ts` and its spec, which still pointed at `packages/mikro-orm/src/` after the files moved to `packages/core/src/query/`.
