---
'@dudousxd/nestjs-telescope-typeorm': minor
---

Add the TypeORM query watcher: a host-wired `Logger` that tees every executed
query into the telescope recorder, with slow-query duration + tagging.
