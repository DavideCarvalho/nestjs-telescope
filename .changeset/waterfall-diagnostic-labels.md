---
'@dudousxd/nestjs-telescope': patch
---

Trace waterfall: diagnostics span entries now render a meaningful `lib:event` label (e.g. `agent:llm.turn`) instead of falling back to the bare entry type ("diagnostic").
